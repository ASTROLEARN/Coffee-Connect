services:
  - type: web
    name: brew-and-co
    env: static
    rootDir: coffee-shop
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
